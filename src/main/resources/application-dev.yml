
spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/sps_db
    password: '1253'
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'
    properties:
      hibernate:
        jdbc:
          time_zone: GMT+3
  security:
    filter:
      order: 10
server:
  port: 8080
aws:
  region: placeholder
  accessKey: placeholder
  secret: placeholder
  violationBucket: placeholder
